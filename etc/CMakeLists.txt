configure_file("default.xml.in" "${CMAKE_PROJECT_NAME_NOSPACE_SMALL}.xml" @ONLY)

set(CONFIG_FILES "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_PROJECT_NAME_NOSPACE_SMALL}.xml")

install(
  FILES ${CONFIG_FILES}
  DESTINATION "${CMAKE_INSTALL_SYSCONFDIR}/${CMAKE_PROJECT_NAME_NOSPACE}"
  COMPONENT lib
)