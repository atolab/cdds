#include "CUnit/Runner.h"

@CUnit_Decls@

int main (int argc, char *argv[])
{
    if (cu_runner_init(argc, argv)) {
        goto err_init;
    }

@CUnit_Defs@

    cu_runner_run();
    cu_runner_fini();
err_init:
    return CU_get_error();
}

